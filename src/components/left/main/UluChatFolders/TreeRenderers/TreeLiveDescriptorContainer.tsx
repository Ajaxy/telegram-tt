import type { ReactNode } from 'react';
import type { TreeConfiguration } from 'react-complex-tree';
import type { FC } from '../../../../../lib/teact/teact';

type OwnProps = {
  children: ReactNode;
  tree: TreeConfiguration;
};

const TreeLiveDescriptor: FC<OwnProps> = () => {
  return undefined;
};

export default TreeLiveDescriptor;
